{
  "scripts": {
    "heroku-prebuild": "export NPM_CONFIG_PRODUCTION=false; export NODE_ENV=; NPM_CONFIG_PRODUCTION=false NODE_ENV=development cd server && npx yarn --only=dev --dev&& npx yarn build && cd ..",
    "start": "cd server && npx yarn start"
  }
}
